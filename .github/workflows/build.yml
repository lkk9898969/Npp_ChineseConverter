name: Build and Release

on: [push, pull_request]

jobs:
  build:
    name: Build (${{ matrix.build_configuration }} ${{ matrix.platform }})
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        platform: [x64, x86, ARM64]
        build_configuration: [Debug, Release]
        include:
          - platform: x64
            cmake_arch: x64
          - platform: x86
            cmake_arch: Win32
          - platform: ARM64
            cmake_arch: ARM64

    steps:
    - name: Checkout repository
      uses: actions/checkout@v7
      with:
        submodules: recursive
    
    - name: Fix OpenCC invalid options in CMakeLists.txt
      run: |
        git apply deps/OpenCC_fix_invalid_options.patch

    - name: OpenCC ARM64 Cross-Compile Hack
      if: matrix.platform == 'ARM64'
      run: |
        git apply deps/OpenCC_cross_compile_hack.patch
        cmake -S deps/OpenCC -B build-native-tools -G "Visual Studio 18 2026" -A x64 -T ClangCL
        cmake --build build-native-tools --config Release --target Dictionaries

    - name: Configure CMake
      shell: pwsh
      run: |
        $cmakeArgs = @("-B", "build", "-G", "Visual Studio 18 2026", "-A", "${{ matrix.cmake_arch }}", "-DCMAKE_BUILD_TYPE=${{ matrix.build_configuration }}", "-T", "ClangCL")
        
        if ("${{ matrix.platform }}" -eq "ARM64") {

          $nativeDictTool = Join-Path (Get-Location) "build-native-tools\src\tools\Release\opencc_dict.exe"
          $nativeOpenccTool = Join-Path (Get-Location) "build-native-tools\src\tools\Release\opencc.exe"

          Write-Host "Using native opencc_dict at: $nativeDictTool"
          Write-Host "Using native opencc at: $nativeOpenccTool"
          
          $cmakeArgs += "-DOPENCC_DICT_BIN=$nativeDictTool"
          $cmakeArgs += "-DOPENCC_BIN_PATH=$nativeOpenccTool"
        }
        
        cmake @cmakeArgs

    - name: Build
      run: cmake --build build --config ${{ matrix.build_configuration }}

    - name: Package
      run: cmake --build build --config ${{ matrix.build_configuration }} --target PACKAGE

    - name: Upload Artifacts
      uses: actions/upload-artifact@v7
      with:
        name: NppChineseConverter-${{ matrix.build_configuration }}-${{ matrix.platform }}
        path: build/Packages/NppChineseConverter-${{ matrix.build_configuration }}-${{ matrix.platform }}.zip
        if-no-files-found: error

  release:
    name: Create Release
    needs: build
    if: startsWith(github.ref, 'refs/tags/')
    runs-on: windows-latest
    permissions:
      contents: write
      
    steps:
      - name: Download Artifacts
        uses: actions/download-artifact@v8
        with:
          path: artifacts
          pattern: NppChineseConverter-Release-*
          merge-multiple: true

      - name: Rename for Release
        run: |
          cd artifacts
          # Rename NppChineseConverter-Release-{arch}.zip to NppChineseConverter-{arch}.zip
          Get-ChildItem "NppChineseConverter-Release-*.zip" | ForEach-Object {
            $newName = $_.Name -replace "-Release-", "-"
            Rename-Item -Path $_.FullName -NewName $newName
          }

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          files: artifacts/NppChineseConverter-*.zip
          generate_release_notes: true
